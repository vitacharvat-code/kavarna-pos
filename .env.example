# Zkopíruj tento soubor jako .env a vyplň hodnoty

# Connection string z Neon.tech (Settings → Connection string)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Prostředí (production na Railway, development lokálně)
NODE_ENV=production
